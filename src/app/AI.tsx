import { azure } from "@ai-sdk/azure";
import { generateText } from "ai";

export default async function AI({ system }: { system: string }) {
  /*const { text } = await generateText({
    model: azure("tenk-ai-workshop-2024"),
    system: system,
    prompt: "Why is the sky blue?",
  });

  console.log(text);*/

  return <></>;
}
